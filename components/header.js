import React from 'react';
import { Input } from './ui/input';

const Header = () => {
  return (
    <header>
      <main>
        <Input />
      </main>
    </header>
  );
}

export default Header;
